// Smoke test placeholder. Substituir por testes reais conforme fases avançam.
//
// Hoje o app depende de Firebase + Google Maps para inicializar — testar
// MapScreen direto exige mock de Firestore e fake do plugin GoogleMap.
// Fica de fora do MVP até termos uma camada de seam clara.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — substituir conforme camadas testáveis aparecerem', () {
    expect(1 + 1, 2);
  });
}
